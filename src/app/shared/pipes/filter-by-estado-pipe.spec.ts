import { FilterByEstadoPipe } from './filter-by-estado.pipe';

describe('FilterByEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
